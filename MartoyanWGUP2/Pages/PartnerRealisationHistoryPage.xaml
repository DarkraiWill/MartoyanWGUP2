<Page x:Class="MartoyanWGUP2.Pages.PartnerRealisationHistoryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MartoyanWGUP2.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PartnerRealisationHistoryPage">

    <Grid>
        <DataGrid x:Name="PartnerProductsDataGrid" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Наименование продукции" Binding="{Binding Product.ProductName}" Width="auto"/>
                <DataGridTextColumn Header="Количество" Binding="{Binding ProductionCount}" Width="auto"/>
                <DataGridTextColumn Header="Дата продажи" Binding="{Binding SalesDate}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
